{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities granted to the main window. Read-only by design — no fs/write or shell-exec permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:default",
    "opener:default"
  ]
}
