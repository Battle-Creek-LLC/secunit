import { Placeholder } from "./Placeholder";

export function Controls() {
  return (
    <Placeholder
      title="Controls"
      description="Every control with status badge; row → detail + recent runs."
      job="JOB-07"
    />
  );
}
